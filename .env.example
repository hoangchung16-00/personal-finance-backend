# Environment Variables for Personal Finance Backend
# Copy this file to configure your deployment

# =============================================================================
# REQUIRED VARIABLES
# =============================================================================

# Rails Environment (use 'production' for deployment)
RAILS_ENV=production

# Rails Master Key (get from config/master.key - DO NOT COMMIT THIS!)
# Run: cat config/master.key
RAILS_MASTER_KEY=your_master_key_here

# Database Connection URL (usually auto-set by hosting platform)
# Format: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://user:password@localhost:5432/personal_finance_production

# Enable static file serving (required for API-only mode)
RAILS_SERVE_STATIC_FILES=true

# Enable stdout logging (required for cloud platforms)
RAILS_LOG_TO_STDOUT=true

# =============================================================================
# OPTIONAL VARIABLES
# =============================================================================

# Server Port (usually auto-set by hosting platform)
# PORT=3000

# Maximum number of threads per Puma worker
# RAILS_MAX_THREADS=5

# Number of Puma workers
# WEB_CONCURRENCY=2

# Database connection pool size
# DB_POOL=5

# Force SSL connections (recommended for production)
# RAILS_FORCE_SSL=true

# Rails log level (debug, info, warn, error, fatal)
# RAILS_LOG_LEVEL=info

# =============================================================================
# PLATFORM-SPECIFIC NOTES
# =============================================================================

# Render.com:
#   - DATABASE_URL is automatically set when you create a PostgreSQL database
#   - PORT is automatically set by Render
#   - Use the "Internal Database URL" for DATABASE_URL

# Railway.app:
#   - DATABASE_URL is automatically set when you add PostgreSQL plugin
#   - PORT is automatically set by Railway
#   - Set variables using: railway variables set KEY=value

# Fly.io:
#   - DATABASE_URL is automatically set when you provision PostgreSQL
#   - Set secrets using: flyctl secrets set KEY=value
#   - PORT is set to 8080 by default on Fly.io

# Heroku:
#   - DATABASE_URL is automatically set by the PostgreSQL addon
#   - PORT is automatically set by Heroku
#   - Set config using: heroku config:set KEY=value

# =============================================================================
# SECURITY WARNING
# =============================================================================

# NEVER commit this file with real values to version control!
# The .env file should be in .gitignore
# Always use environment variables or secrets management in production
